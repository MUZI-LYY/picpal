# 输入
- 当前景点 POI
- 用户出片偏好
- RAG 返回的候选 PhotoSpot 列表

# 任务
从候选列表中选择最多 3 个最匹配的机位并给出简短理由。

# 规则
- 不得新增候选列表之外的机位。
- spot_id、poi_id、坐标、图片、位置说明、来源和 best_time 原样保留。
- 只选择 ingestion_status=auto_verified 且达到最低置信阈值的记录。
- 不得把景点中心坐标当作摄影者站位。
- 没有合格候选时返回空数组。

# 输出
返回所选机位的 spot_id 列表与简短理由。
